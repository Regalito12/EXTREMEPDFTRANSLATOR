// Configuration for PDF Translator

const CONFIG = {
    // API_BASE_URL: The URL of your backend server.
    // For local development, leave it as an empty string '' (uses relative path).
    // For InfinityFree output, set it to your Render/Railway backend URL (e.g., 'https://my-app.onrender.com').
    API_BASE_URL: ''
};
